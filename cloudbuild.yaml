steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - hello_world
  - --source=.
  - --trigger-bucket=sceg_karthik
  - --runtime=python37
  - --allow-unauthenticated
